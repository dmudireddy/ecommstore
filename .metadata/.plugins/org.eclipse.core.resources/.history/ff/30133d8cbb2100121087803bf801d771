require 'spec_helper'

describe Post do
  before  do
    @valid_attributes = 
    {
      :title => "Hello World",
      :body => "Goood Morning"
    }
  end
  it "creates a new post" do
    @post.should(be_an_instance_of Post)
    
  end
end
